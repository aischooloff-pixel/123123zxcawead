# Промпт для создания Telegram-бота по продаже прокси

## Общее описание
Создай Telegram-бота на Python (библиотека `aiogram 3.x`) для автоматизированной продажи прокси. Бот должен интегрироваться с API **Proxy6 (px6.me)** для закупки прокси и **CryptoBot** для пополнения баланса пользователей.

## Основной функционал (на основе анализа видео)

### 1. Интерфейс и навигация
- **Главное меню (Reply Keyboard):**
  - `📲 Купить` — переход к выбору параметров прокси.
  - `🖥 Профиль` — информация о пользователе, балансе и активных прокси.
  - `ℹ️ Информация` — справочный раздел.
- **Дизайн:** Использовать эмодзи в кнопках и сообщениях, как в оригинале. Приветственное сообщение должно содержать баннер (изображение) и текст "Добро пожаловать! Ваш баланс: [сумма] RUB".

### 2. Процесс покупки (Inline-меню)
- **Выбор страны:** Динамический список стран, получаемый через метод `getcountry` API Proxy6.
- **Параметры:**
  - Тип прокси (IPv4, IPv6, IPv4 Shared).
  - Период (выбор из доступных дней, например, 3, 7, 30 дней).
  - Количество (ввод числа или выбор).
- **Подтверждение:** Перед оплатой выводить резюме заказа: Тип, Страна, Количество, Период и итоговая стоимость (через `getprice`).
- **Выдача:** После успешной покупки (списания с внутреннего баланса) бот отправляет данные прокси: IP, Порты (HTTP/SOCKS5), Логин, Пароль и дату окончания.

### 3. Интеграция с API Proxy6 (px6.me)
- Базовый URL: `https://px6.link/api/{api_key}/`
- **Методы для реализации:**
  - `getcountry`: получение списка доступных стран.
  - `getprice`: расчет стоимости перед покупкой.
  - `buy`: совершение покупки.
  - `getproxy`: получение списка активных прокси для раздела "Профиль".
  - `balance`: (автоматически возвращается в любом ответе) синхронизация баланса админа.

### 4. Пополнение баланса через CryptoBot
- Использовать **Crypto Pay API**.
- **Процесс:**
  1. Пользователь нажимает "Пополнить баланс" в профиле.
  2. Вводит сумму в рублях.
  3. Бот создает счет через `createInvoice` (параметры: `fiat='RUB'`, `currency_type='fiat'`).
  4. Бот выдает ссылку на оплату.
  5. После оплаты (проверка через Webhook или polling `getInvoices`) баланс пользователя в БД бота увеличивается.

### 5. Личный кабинет (Профиль)
- Отображение ID пользователя.
- Текущий баланс в RUB.
- Кнопка "Пополнить баланс".
- Список купленных прокси с указанием срока действия.

## Технические требования
- **Язык:** Python 3.10+
- **База данных:** SQLite или PostgreSQL (для хранения балансов пользователей, истории заказов и состояния инвойсов).
- **Архитектура:** Модульная структура (handlers, services, keyboards, states).
- **Безопасность:** Хранение API ключей в `.env` файле. Обработка ошибок API (например, если прокси данного типа закончились в выбранной стране).

## Логика работы баланса
- У бота есть "внутренний баланс" пользователя.
- Пополнение через CryptoBot увеличивает этот баланс.
- Покупка прокси через API Proxy6 списывает средства с внутреннего баланса пользователя (при этом у админа должны быть средства на основном аккаунте Proxy6).
